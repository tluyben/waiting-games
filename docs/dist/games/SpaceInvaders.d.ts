import { GameEngine } from '../GameEngine';
import { GameConfig } from '../types';
export declare class SpaceInvaders extends GameEngine {
    private player;
    private bullets;
    private invaders;
    private shields;
    private stars;
    private score;
    private lives;
    private highScore;
    private gameState;
    private invaderDirection;
    private invaderMoveTimer;
    private invaderAnimTimer;
    private shootCooldown;
    private explosions;
    private readonly DESIGN_WIDTH;
    private readonly DESIGN_HEIGHT;
    constructor(container: HTMLElement | string, config?: GameConfig);
    private createStars;
    private initGame;
    private createInvaders;
    private createShields;
    protected handleKeyDown(event: KeyboardEvent): void;
    protected handleKeyUp(event: KeyboardEvent): void;
    protected handleTouchStart(event: TouchEvent): void;
    protected handleTouchEnd(event: TouchEvent): void;
    private playerShoot;
    private invaderShoot;
    protected update(): void;
    private checkShieldCollisions;
    private updateInvaders;
    private checkBulletCollisions;
    private checkCollision;
    protected render(): void;
    private drawPlayer;
    private drawInvader;
    private drawSquidInvader;
    private drawCrabInvader;
    private drawOctopusInvader;
    private drawExplosion;
    private drawHUD;
    private drawEndScreen;
    private drawMobileControls;
}
